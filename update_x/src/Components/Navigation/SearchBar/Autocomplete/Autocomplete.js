import React, { Fragment } from "react";
import classes from "./Autocomplete.module.css";

const Autocomplete = (props) => {
  return (
    <Fragment>
      <h2>This is autocomplete</h2>
    </Fragment>
  );
};

export default Autocomplete;
